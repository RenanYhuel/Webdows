import './notepad.css';

const Notepad = () => {
    return (
        <div className="notepad-container">
            <textarea className="notepad-textarea"></textarea>
        </div>
    );
};

export default Notepad;
